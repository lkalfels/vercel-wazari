export function AnnouncementBar() {
  return (
    <div className="bg-primary text-background text-center text-xs sm:text-sm font-medium py-2 px-4">
      Aula experimental + avaliação inicial — vagas limitadas por turma.
    </div>
  );
}
